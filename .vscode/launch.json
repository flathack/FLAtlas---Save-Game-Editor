{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Savegame Editor (standalone)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/start_savegame_editor.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}
